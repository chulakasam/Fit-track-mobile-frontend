import {useLocalSearchParams, useRouter} from "expo-router";
import {useState} from "react";
import {StyleSheet,Button, FlatList, TouchableOpacity, View} from "react-native";

export default function SelectItem() {

    return (
        <View>Item</View>
    );
}

const styles = StyleSheet.create({
    item : {
        padding : 15,
        backgroundColor : "#ddd",
        marginBottom : 10,
        borderRadius : 5
    }
})